# T8star-Aix IndexTTS 2.5 开发路线图

本文档记录桌面整合包与 ComfyUI 节点的共同开发范围、依赖政策和验收标准。当前公开基线为
**Desktop 0.23.0 / ComfyUI Node 0.22.0**。

| 发行物 | 当前版本 | 本版主要内容 |
| --- | --- | --- |
| Windows Electron 整合包 | 0.23.0 | 持久队列、A/B 候选、长文本预检、显存策略、分层更新与时间轴重排 |
| ComfyUI V3 节点 | 0.22.0 | 数字/日期归一化自检、逐句控制、兼容回退与 33 组工作流 |
| 固定官方核心 | `ee40fa7d` | 未发现必须更换的 revision |
| 固定模型镜像 | `14166a74` | Hugging Face 独立下载，26 个主模型与辅助文件全部校验 |

## Node v0.21.4 / Desktop v0.22.3

- [x] 识别 NVIDIA 环境中的 `waves_per_eu` PyTorch/Triton 不兼容错误，并给出明确诊断。
- [x] Desktop 流式试听接入与普通生成一致的加速失败回退，自动重载普通模型完成任务。
- [x] 高显存显卡默认使用均衡安全配置，实验性 GPT 加速保留为手动选择。
- [x] Desktop 真实开启 GPT 加速生成有效 WAV；两端新增精确错误回归测试。

## Desktop v0.22.0 / Node v0.21.0（上一公开基线）

- [x] 每个内部长文本分段记录语言感知文字单位、真实时长和单位/秒。
- [x] 至少两个稳定分段后建立中位语速基线，仅在后续分段低于基线 45% 时标记异常。
- [x] 只用更小 Token 上限和独立 seed 重做异常段；候选未明显改善或变得过快时保留原段。
- [x] 普通情绪放慢、短句、确定性采样和原生目标时长模式不强行调整。
- [x] Desktop 报告和 ComfyUI 状态均输出可审计的跨段语速明细。

## Desktop v0.23.0 / Node v0.22.0

- [x] 桌面更新拆成签名应用层与独立运行库层；运行库按小于 2 GiB 分卷，支持续传、分卷/整包/逐文件校验、健康检查和回滚，模型继续独立放在 Hugging Face。
- [x] 参考音频加入自动质量摘要与按需裁剪；显存策略支持手动释放、默认空闲 10 分钟释放、按生成次数重载和下次生成自动恢复。
- [x] 长文本生成前显示实际归一化文本、Token 分段、预计时长和风险，用户确认后才应用到正文。
- [x] 单句、多角色与 SRT 统一进入持久 FIFO 队列；临时参考音频复制到队列资产目录，异常退出可恢复，失败/取消可重试。
- [x] 追加候选提供折叠的 A/B/C/D 试听、1–5 星评分、备注和持久收藏。
- [x] 多角色固定操作条显示实时进度；重复生成不再把空时间强制变成 `0/0`；台词支持上移/下移并保持 SRT 时间槽，内容变更时禁止误用旧音频直接重混。
- [x] 所有高级、诊断、安装、候选和工程区域默认折叠，常用生成/停止操作保持固定可见。
- [x] 中、英、日、西、阿固定长文本真实模型回归；本机 RTX 实测 5 组 WAV 全部无削波，严格门禁通过，RTF 为 0.552–0.864，峰值显存不超过 5.77 GB。
- [x] `quality-report.json` 记录可选 CER/WER、跨段语速、削波、静音、时长、RTF 与峰值显存，并支持旧报告基线比较。
- [x] Desktop 提供跨段语速柱状图、可读表格、原始/自动重试/当前采用三路试听，以及内部单段重做后安全重组最终 WAV。
- [x] `indextts.cli` 完成正式 2.5 五语言、情感、时长、采样、CFM、精度、参考设备与可选加速参数，并通过独立真实 BF16 WAV 冒烟。
- [x] 根仓库 183 项测试通过（另 1 项按环境跳过）；compileall、Pylint Fatal 门禁、Electron 五组脚本测试、打包与包内运行时自检全部通过。
- [x] 历史规划文档和中英文使用说明同步为当前架构状态。

## Node v0.21.1 / Desktop 开发分支

- [x] 固定 OpenAI Whisper `20250625`，使用 `base` CUDA 完成中英日西阿真实 CER/WER 基线。
- [x] 定时 GPU 质量回归与普通 CI 完全分离，避免 PR/Push 流程加载大模型。
- [x] 显式 `GenerationMixin` 继承，并以 TorchCodec 原生 I/O 兼容 Torchaudio 2.9+。
- [x] 节点版本提升到 0.21.1，重新生成并校验 33 组 UI/API 工作流。

## Node v0.21.2 / Desktop v0.22.1

- [x] Arabic Whisper `small` 实测 WER 由 0.6154 降至 0.1923，加入不掩盖辅音错误的保守规范化。
- [x] Transformers GPT 缓存迁移到 `DynamicCache`，保留旧 tuple 输入兼容并完成真实权重回归。
- [x] 启动前检查 TorchCodec 与 Windows FFmpeg 共享 DLL；Torchaudio 2.8 便携路径不变。
- [x] RTX 真机完成正式 8GB/24GB 五语言回归；生成两档脱敏基线和 CER/WER、RTF、VRAM 趋势产物。

## Desktop v0.21.2 / Node v0.20.9

- [x] Desktop 音色包与项目包改为事务导入/原子导出，失败时不留下半成品或污染现有音色库。
- [x] 拒绝 Windows 大小写、Unicode 归一化或尾随空格下会互相覆盖的压缩包路径。
- [x] 保存音色统一解码为单声道 24 kHz PCM WAV，并拒绝无有效采样的输入。
- [x] Desktop audio.cpp 可选组件严格限制在用户数据目录内，清单、大小和 SHA-256 缺一不可。
- [x] 启动器版本展示同步到 Desktop 0.21.2 / Node 0.20.9。
- [x] Node 0.20.9 增加 Pylint 4 Fatal/Astroid CI 门禁，完成 Linux、Windows 双平台测试并在 Registry 激活。

## Desktop v0.21.0 / Node v0.20.0（历史）

- [x] 音色库新增标签、搜索、收藏、备注、质量元数据与 `.t8voice.zip` 导入导出。
- [x] ComfyUI 新增“已保存音色”节点，直接读取 Desktop 音色包，无需重复上传参考音频。
- [x] Desktop 完整项目包携带任务状态、逐句/合并音频、字幕报告与引用音色，可跨机器恢复。
- [x] Desktop 与当时的 ComfyUI 版本均提供主动确认的 audio.cpp Windows 运行时/GGUF 下载、续传、磁盘与哈希校验。
- [x] 当时 ComfyUI 示例扩充到 34 组 UI/API 工作流；Node 0.20.5 后已删除 Registry 不允许的一键联网安装示例，现保留 33 组。

## v0.20.0 / Node v0.19.0

- [x] Electron 启动器显示模型扫描、下载和 SHA-256 校验的总体及阶段进度。
- [x] 下载卡片显示当前文件、传输字节、实时速度、预计剩余时间和磁盘空间预检结果。
- [x] Hugging Face 继续使用固定 revision 和断点续传；损坏文件强制刷新，失败原因结构化回传到启动器。
- [x] ComfyUI 模型加载节点同步接入可中断任务进度，下载完成后校验全部 26 个文件。
- [x] 版本基线、路线图和中英文说明同步，全部 32 组节点 UI/API 示例在发布前重新生成。

## v0.19.0

- [x] GitHub Releases 提供小型应用层增量包，不重复分发 Python、CUDA 运行库和模型。
- [x] Ed25519 验证清单，并校验 Release 资产大小、ZIP SHA-256 与逐文件 SHA-256。
- [x] 下载支持断点续传；安装前再次校验，启动健康检查失败则自动恢复备份。
- [x] 启动器提供 stable/beta 通道、每日只读检查、手动下载/安装和清晰的签名状态。
- [x] 模型与程序分离，固定到 `t8star/IndexTTS-2.5-Comfy@14166a74`，下载后完整校验。
- [x] Windows 源码增量包可由 GitHub Actions 构建、签名并上传到对应 Release。

## v0.18.1

- [x] “语音生成”页新增已保存角色音色选择器，选中后自动载入音色参考音频。
- [x] 保存角色后自动同步到单句生成页，并提供手动刷新；上传、拖拽和录音入口继续保留。
- [x] 明确只复用音色音频，不覆盖本页语言、情感和高级生成参数。
- [x] `ffprobe.exe` 缺失问题继续由 0.18.0 起采用的包内 PyAV 解码/编码路径规避。

## v0.18.0

- [x] 长英文、西班牙语和长 SRT 逐句检测 `max_mel_tokens` 与异常音频时长，必要时自动缩短 Token 分段并重试一次。
- [x] 使用真实 IndexTTS 2.5 模型完成长英文/西语 WAV 回归，并修复 Windows GBK 控制台打印西语字符导致的生成中断。
- [x] 桌面多角色时间轴支持拖动整段、左右手柄缩放、邻接台词/ASR 逐字时间点吸附，以及 Alt 临时禁用吸附。
- [x] 拖动结果自动回写表格并选中台词，可单独重做该段后自动重新合并，其他逐句 WAV 继续复用。
- [x] 桌面与 ComfyUI 可查看参考条件缓存条目、容量、命中/未命中和命中率；安全清理只删除本项目的 `safetensors` 缓存。
- [x] Desktop/Node 版本统一提升到 0.18.0，并在推送前刷新全部 32 组 UI/API 示例。
- [x] 本版不扩展 GPU 压测矩阵，也不增加 Linux sidecar 服务。

## v0.17.0

- [x] 新增共享上下文情感分析器，前文、目标句和后文明确分区，并避免把其他角色的情感混入目标句。
- [x] 输出固定八维“喜、怒、哀、惧、厌恶、低落、惊喜、平静”向量与建议强度。
- [x] 桌面端把建议写入现有可编辑时间轴，保留人工逐句情感；确认前绝不自动合成。
- [x] ComfyUI 新增只分析、不生成音频的上下文逐句情感建议节点。
- [x] 新增第 32 组两阶段 UI/API 工作流，先分析和预览，用户确认后再接生成节点。
- [x] 桌面 99 项、节点 151 项、Comfy V3 schema、Electron 启动器和真实 QwenEmotion 权重抽样验收。
- [x] Desktop/Node 版本统一提升到 0.17.0，并在推送前重新生成全部工作流。

## 基本原则

- 只支持 IndexTTS 2.5，不回退到 2.0。
- 桌面整合包与 ComfyUI 节点共享同一套脚本、时间轴和加速语义。
- 基础安装必须在没有 DeepSpeed、FlashAttention、Triton 等附加包时正常启动和推理。
- 所有加速均为用户主动选择的可选能力；不可用或初始化失败时自动回退，并明确显示原因。
- `requirements.txt` 不加入 DeepSpeed 等难安装依赖；桌面整合包单独内置精确 ABI 轮子，DeepSpeed 永不默认启用。
- SRT 时长适配属于尽量贴合，必须报告超时，不能宣称逐帧精确对齐。

## v0.6.0 开发范围

### 生成控制升级

- [x] 五语种自动 Token 上限、手动覆盖及生成前分段/停顿预览。
- [x] 标点停顿预设、自定义毫秒停顿和 `<pause=0.5>` 显式停顿。
- [x] 长文本/多停顿块 GPT 加速 KV Cache 风险自动保护（v0.8.1 已由根修复替代）。
- [x] 目标秒数自然适配、补静音和强制精确裁剪。
- [x] 人声清晰、清晰旁白、去刺耳、温暖和峰值归一化后处理。

### 上游同步与诊断

- [x] 将模型清单更新到官方 IndexTTS-2 2.5 配置版本（权重未变化时只修复 `config.yaml`）。
- [x] 加入长文本分段预览：显示段号、文本、token 数和风险提示。
- [x] 加入运行环境、模型版本、实际加速状态和回退原因诊断。
- [x] 记录生成耗时、音频时长、实时率 RTF 和 CUDA 峰值显存（可获得时）。

### 角色音色库

- [x] 桌面端可新建、覆盖/改名保存、删除和试听角色音色。
- [x] 每个角色保存名称、参考音频、默认语言和可选情感/发音设置。
- [x] 每个角色可独立保存跟随音色、情感参考音频、八维向量或文本描述；情感音频随角色复制到用户目录。
- [x] 音色文件复制到用户数据目录，项目移动或整合包升级后仍可使用。
- [x] ComfyUI 提供可连线的角色音色与角色库类型，不依赖桌面端数据库。
- [x] ComfyUI 提供 `Merge Voice Emotions` 搜索兼容节点，并保留每个角色各自的 emotion 配置。

### 多角色与批量台词

- [x] 支持 `角色|台词|语言` 批量文本，空行和 `#` 注释可忽略。
- [x] 支持 JSON 台词数组，逐句语言、语速和角色覆盖。
- [x] 未映射角色在生成前报出明确位置，不静默使用错误音色。
- [x] 输出合并音频、逐句音频、逐句时间范围和 JSON 报告。

### SRT 字幕配音

- [x] 兼容 UTF-8 BOM、CRLF、多行字幕及逗号/点毫秒格式。
- [x] 支持字幕文本中的 `[角色] 台词`、`角色: 台词` 两种角色标记。
- [x] 支持 `顺延` 和 `时间轴混音` 两种冲突策略。
- [x] 可选“适配字幕槽位”：最多二次推理并将时长系数限制在 0.5–2.0。
- [x] 报告每条字幕的实际时长、目标槽位、超时量和最终开始/结束时间。

### 可选加速

- [x] 自动探测 BF16、BigVGAN CUDA kernel、Torch Compile、GPT 加速、DeepSpeed 的依赖与工具链。
- [x] 桌面端和 ComfyUI 均可选择 `关闭/自动安全/BigVGAN CUDA/Torch Compile/GPT 加速/DeepSpeed`。
- [x] 缺少可选依赖时不影响普通模式，选择后给出回退说明。
- [x] GPT 加速遇到不兼容采样参数时自动使用普通 GPT 路径；合成提示 KV Cache 已在核心中修复。
- [x] v0.9.0 桌面整合包内置 Python 3.10 / torch 2.8.0+cu128 精确匹配的 DeepSpeed、FlashAttention 与 Triton Windows 轮子；仍保持手动选择和失败回退。
- [x] vLLM-Omni 记录为后续可选服务端后端，不污染 Windows 本地运行时。
- [x] TensorRT 仅在官方提供 IndexTTS 2.5 引擎后接入；当前官方后端仅支持 2.0，不作虚假支持。

### 桌面整合包

- [x] 新增“角色音色库”“批量台词”“SRT 配音”“环境与加速”可见入口和使用说明。
- [x] 支持导出 WAV、逐句文件与包含报告的 ZIP。
- [x] Electron 启动器继续显示 `B站：T8star-Aix`，并保存用户设置。
- [x] 新模块和模型清单纳入 Electron `extraResource` 与打包完整性检查。

### ComfyUI V3 节点

- [x] 新增角色音色、角色库、脚本解析、多角色/SRT 生成和环境诊断节点。
- [x] 保持标准 `AUDIO` 输入输出，并提供机器可读 JSON 报告。
- [x] 加入多角色、批量台词、SRT、可选加速 UI/API 示例工作流。
- [x] 保持 Transformers 4.52.1–4.57.6 兼容，基础依赖尽量精简。

## 验收标准

- [x] 桌面端解析/时间轴/混音/音色库/加速与新生成控制测试全部通过（35 项）。
- [x] ComfyUI V3 节点 schema、18 组 UI/API 示例和当前依赖测试通过（51 项）。
- [x] Transformers 4.57.6 独立兼容环境通过 46 项，1 项按环境条件跳过。
- [x] 无任何 DeepSpeed、FlashAttention、Triton 的基础环境可加载并完成普通 BF16 推理。
- [x] BigVGAN CUDA 融合核完成真实 GPU 编译/推理；缺依赖、初始化失败和运行失败回退测试通过。
- [x] 桌面端完成单句、多角色、批量/SRT 解析合成、双停止任务和历史记录冒烟测试。
- [x] Electron 0.6.0 便携目录重建，打包后 Python/CUDA/Gradio/Transformers/AAC/中文路径自检通过。
- [x] IndexTTS 2.5 真实模型完成两段生成、前导/段间停顿、目标时长和人声后处理联合冒烟验证。
- [x] README 同步功能、格式、限制、可选依赖和示例，不把可选依赖写成必装。
- [x] v0.6.0 变更已提交并推送到 ComfyUI 仓库 `main`。（历史记录）

## v0.7.0 开发范围

- [x] 桌面端与 ComfyUI 暴露 CFM 扩散步数、CFG 引导强度和噪声温度，默认值保持官方行为。
- [x] 桌面端新增固定 seed、流式试听、当前任务取消和最终 WAV 独立保存。
- [x] 原生 `target_duration` 一次推理适配整合包、单节点和 SRT 多角色链路，并保留旧核心回退。
- [x] 多角色任务逐句写入 `task.json`，支持重启续做和指定单句重生成。
- [x] ComfyUI 示例扩展为 19 组 UI/API，新增 CFM 高级参数示例并更新目标时长示例。
- [x] 桌面端 40 项、ComfyUI 50 项自动化测试通过；Transformers 4.57.6 环境 50 项通过、2 项按环境跳过。
- [x] 真实 2.5 权重完成固定 seed、自定义 CFM、流式返回及原生 2.5 秒目标时长联合冒烟。
- [x] Electron 0.7.0 便携目录重建，并通过内置 Python/CUDA/Gradio/Transformers/AAC/中文路径自检。
- [x] README、示例工作流与版本清单复核完成。
- [x] 变更已提交并推送 ComfyUI `main`。

## v0.8.0 开发范围

- [x] 桌面端与 ComfyUI 新增本地 Whisper ASR 校对，输出识别文本、编辑距离、CER、相似度和阈值判定。
- [x] Whisper 直接接收解码后的波形，不依赖系统 FFmpeg；ASR 保持可选，不污染基础 TTS 依赖。
- [x] 多角色生成逐句保存 ASR 报告，并支持按“全部识别结果/仅校对通过”回写字幕文本。
- [x] 支持保留原 SRT 时间或使用生成音频真实时间轴，导出可直接使用的 `rewritten.srt`。
- [x] 桌面端加入毫秒级可编辑时间轴表与可视化轨道，保存任务可不重新生成直接重混。
- [x] ComfyUI 新增时间轴编辑、ASR 校对和字幕回写节点，时间轴提供标准 IMAGE 彩色轨道预览，示例扩展为 22 组 UI/API 工作流。
- [x] 桌面端 46 项、ComfyUI 53 项（2 项按环境跳过）及 Transformers 4.57.6 回归通过。
- [x] 真实 IndexTTS 音频完成 Whisper tiny/base CUDA 校对，低相似度结果按阈值正确保留原文。
- [x] Electron 0.8.0 便携包重建，包内 ASR/时间轴、CUDA、AAC 和中文路径自检通过。

## v0.8.1 开发范围

- [x] 回移上游 GPT 合成提示 KV Cache 根修复，长文本和显式停顿不再强制关闭 GPT 加速。
- [x] ASR 增加 `auto / openai_whisper / faster_whisper` 后端选择；faster-whisper 保持可选。
- [x] ASR 增加简繁体、中文数字和标点归一化，中文/日文报告 CER，其余语言报告 WER。
- [x] ASR 报告增加逐项差异和词级时间戳，桌面端直接显示差异表，ComfyUI 增加时间戳输出。
- [x] Electron 打包按明确清单剔除未参与运行的 PyTorch 静态开发库，保留 CUDA/C++ 扩展所需头文件和导入库。
- [x] 桌面 50 项、ComfyUI 57 项、Transformers 4.57.6、V3 API 12 项审计通过。
- [x] 精简包完成 RTX 5090 上 BigVGAN CUDA 全新编译、真实生成与 Whisper CUDA 词级时间戳校对。
- [x] Comfy Registry v0.8.1 发布验收。（历史记录）

## 后续候选

- vLLM-Omni Linux/服务端 sidecar，用于多人并发与吞吐优先场景。
- 波形缩略图、逐字时间戳和人工拖拽吸附；当前 v0.8.0 为逐句毫秒级时间轴编辑。
